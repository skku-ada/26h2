# 1장. Deep Learning 기반 Data 분석 소개

> **응용데이터분석 (Applied Data Analysis) — 딥러닝 기반 데이터 분석**
> 데이터사이언스융합학과 · Rev 1.0
> 원본 슬라이드: 6, 29 ~ 30 (3장)

**학습 목표**

- 전통적 Data 분석과 DL 기반 Data 분석의 관점 차이를 설명할 수 있다.
- Deep Learning 모델을 정답을 맞히는 예측기가 아니라, 복잡한 Data 구조를 드러내는 **분석 도구(계측기)** 로 바라보는 관점을 습득한다.
- 데이터를 하나의 분포가 아니라 **여러 생성 규칙의 혼합**으로 보는 관점을 수식으로 표현하고, 그 관점이 필요한 상황을 판별할 수 있다.
- 전통적 분석과 DL 기반 분석을 잇는 4단계 분석 절차를 구체적인 사례에 적용할 수 있다.
- Deep Learning을 계층적 표현 학습(Layered Representation Learning)으로 설명하고, 인공 뉴런·로지스틱 회귀·Softmax·Cross Entropy·오차 역전파가 그 안에서 각각 어떤 역할을 하는지 정리할 수 있다.

**이 장의 구성**

- 1.1 Deep Learning 기반 Data 분석
- 1.2 Deep Learning 개요

**이 장이 답하는 질문**

- 전통적 분석은 현실 데이터 앞에서 왜 자주 무너지는가?
- 학습 결과에 나타나는 오류(error)와 혼동(confusion)은 모델의 실패인가, 아니면 데이터가 보내는 신호인가?
- DL은 데이터의 무엇을 보여주고, 무엇을 보여주지 못하는가?
- Deep Learning은 결국 무엇을 학습하는가? 정답인가, 좌표계인가?

이 장은 강의 전체의 관점을 세우는 자리이다. 1.1절은 **분석의 관점**을, 1.2절은 그 관점을 뒷받침하는 **Deep Learning의 기본 개념**을 다룬다. 두 절을 마치면 이후 장에서 다루는 관측 공간·표현 공간·출력 공간·오류 공간을 하나의 흐름으로 읽을 수 있다.

---

## 1장 정리

1장에서 세운 관점을 다섯 갈래로 정리한다.

> **1장 정리**
>
> **① 관점의 전환: 모델이 아니라 데이터**
>
> - Deep Learning을 예측 성능 향상 도구가 아니라 → **데이터 구조를 드러내는 분석 도구(instrument)** 로 사용한다.
> - 오류(error)와 혼동(confusion)은 모델 실패가 아니라 → **데이터 구조의 모순, 가설 충돌의 신호**이다.
>
> **② 전통적 분석 vs DL 기반 분석**
>
> - 전통적 데이터 분석: 관측 변수의 통계량(평균, 분산, 상관)에 의존하며, 단일 분포 가정과 저차 통계 요약 위에 서 있다.
> - DL 기반 데이터 분석: 학습된 표현(representation)과 출력 분포를 분석하여, 데이터의 생성 구조와 잠재적 혼합을 해석한다.
>
> **③ 데이터 생성 관점의 확장**
>
> - 데이터는 하나의 분포가 아니라 → 여러 생성 메커니즘의 혼합·조건부 구조의 결과이다.
> - DL은 명시적 분해 없이도(목적함수에 따라) → **표현 사상(Representation Mapping)** 을 통해 구조를 암묵적으로 노출한다.
>
> **④ Deep Learning의 본질**
>
> - Deep Learning = **Layered Representation Learning**
> - 계층적 변환을 통해 저차 특징 → 고차 특징으로 나아간다.
> - 학습 과정은 정답을 외우는 과정이 아니라, **데이터를 해석 가능한 좌표계로 변환하는 과정**이다.
>
> **⑤ 출력의 재해석**
>
> - 분류 출력은 값이 아니라 **확률 분포**이다.
> - Softmax: score(logit) → 분포
> - Cross Entropy: 정답 분포와 예측 분포의 차이
> - 출력 분포는 불확실성과 구조적 모호성을 포함한다.

> **1장 결론**
> Deep Learning 기반 데이터 분석은 관측된 통계를 설명하는 것이 아니라,
> 학습된 표현과 출력 분포를 통해 **데이터의 생성 구조와 모순을 드러내는 접근**이다.
