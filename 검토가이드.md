# 강의 음성 기반 교재 보완 — 검토 가이드

**작성:** 2026-08-27 · 제안 **183건** · 대상 **교재 md 17개**

---

## 1. 무엇이 들어갔나

교재 md 는 PPT 슬라이드에서 만들어졌다. 그래서 **슬라이드에 적히지 않고 교수님이 말로만 설명한 것**이
빠져 있었다. 특히 그림과 실습 실행결과의 해석이 그렇다. 그 자리를 전사문 근거로 채운 제안이다.

### 착수 전 진단 (기계 측정)

| 항목 | 전체 | 설명 60자 미만 |
|---|---|---|
| 그림 | 487 | **336 (69%)** |
| 실행결과 표 | 91 | **65 (71%)** |

교재가 제대로 설명한 그림 151개의 해석 문단은 **중앙값 119자(사분위 88~168자)** 였다.
이것을 분량 규격으로 삼았다. 실제 제안 183건은 **중앙값 140자, 범위 113~170자**로 그 안에 들어 있다.

### 유형별

| 유형 | 건수 | 뜻 |
|---|---|---|
| 그림해설 | 104 | 그림 뒤에 캡션만 있던 자리 |
| 결과해석 | 40 | 표·출력 뒤에 "그래서 무엇을 뜻하는가"가 없던 자리 |
| 개념보강 | 25 | 교재에 불릿만 있고 강의가 이유·비유를 설명한 자리 |
| 실무맥락 | 14 | 강의에만 있는 조언·주의 |

---

## 2. 검토 방법

각 제안은 이렇게 감싸여 있다.

```markdown
<!-- +강의 id=2.1_관측공간의한계-02 w=w2 ts=[62:09] [62:42] 유형=결과해석 채택=? -->
평균만 놓고 보면 같은 숫자 쪽이 더 가깝고 다른 숫자 쪽이 더 멀어, 거리로 둘을 구분할 수 있을 것처럼
읽힌다. 그러나 이 차이는 평균에서만 나타나는 것이며 …
<!-- /+강의 -->
```

- `w=w2 ts=[62:09]` — **근거가 된 강의 영상과 시각**이다. 미심쩍으면 원 강의를 그 시각에서 확인하면 된다.
  전사문은 `ADA26h1영상/<영상폴더>/_작업/transcript_paragraphs.txt` 에 같은 `[mm:ss]` 로 들어 있다.
- 판정은 **마커 줄 끝 한 글자**만 고친다.

| 판정 | 고칠 것 | 결과 |
|---|---|---|
| 채택 | `채택=?` → `채택=y` | 마커만 사라지고 본문이 교재에 남는다 |
| 기각 | `채택=?` → `채택=n` | 블록 전체가 사라진다 |
| 보류 | 그대로 둔다 | '미검토'로 보고된다. 나중에 이어서 하면 된다 |

블록을 직접 지우지 마라. 판정만 바꾸면 스크립트가 처리한다.

### 정리 실행

저장소 루트에서:

```bash
python3 apply_lecture.py --dry-run     # 고치지 않고 집계만
python3 apply_lecture.py               # 실제 반영
python3 apply_lecture.py ch05_오류공간/5.1_오류구조.md   # 한 파일만
```

미검토가 남아 있으면 그 파일의 안내 주석은 그대로 유지된다. 다 정하고 한 번 더 실행하면 사라진다.

---

## 3. 되돌리기

제안은 **교재 md 원본을 덮어써서** 들어갔다. 그림 상대경로가 살아 있어야 미리보기로 검토할 수 있기 때문이다.
전부 되돌리려면:

```bash
git checkout -- .          # 제안 삽입 전 상태로
```

검토를 시작하기 전에 브랜치를 따두면 안전하다.

```bash
git switch -c lecture-merge
git add -A && git commit -m "강의 음성 기반 보완 제안 183건 삽입 (검토 전)"
```

> ⚠️ git 명령은 **Mac 터미널에서 직접** 실행해야 한다. Cowork 의 device_bash 가 보는 마운트에서는
> `.git/index.lock` 을 지울 수 없어 git 이 동작하지 않는다(이번 세션에서 확인).

---

## 4. 제안이 없는 자리에 대하여

395개 공백 후보 중 183건만 채웠다. **나머지는 강의 음성에 근거가 없어서 비워 뒀다.**
일반 지식으로 그럴듯하게 메우지 않는 것이 이 작업의 원칙이었다.

근거가 없는 이유는 두 가지다.

1. **녹화가 없다.** 수업은 비대면 영상 + 대면으로 구성되고 녹화는 비대면분만 있다.
   예: 3.2 절의 실습 그림 18개는 강의가 `[109:38] 수요일에 코드를 보겠습니다` 로 끝나
   실습이 대면수업에서 진행됐다. 5.2 절 후반부(Dendrogram·Calibration)도 같은 사정이다.
2. **강의가 그 그림을 말로 다루지 않았다.**

→ **빈 자리는 "안 가르친 것"이 아니라 "음성 소스가 없는 것"이다.** 교수님이 직접 채우실 자리다.

---

## 5. 방법 메모 (재현·확장용)

- **슬라이드 번호에 의존하지 않는다.** 강의 후 PPT 에서 장·절 표지 1페이지가 삭제되어
  번호가 어긋난 구간이 있다(사용자 확인). 소절↔강의 구간 대응은 **어휘 IDF 가중 유사도 검색**으로 만들었다.
- **단조 정렬만으로는 부족하다.** 2.1 절의 실습은 교재에서 2.1.1 아래 있으나 강의는 비선형 구조를
  다룬 뒤 58분에 진행했다. 순서 정렬만 썼다면 놓쳤을 자리다. 검색을 주 수단, 정렬을 보조로 썼다.
- **STT 오인식을 교정해 썼다.** `변동 개수`→변동계수, `피소성`→희소성, `에라`→Error,
  `러닝메이트`→Learning Rate, `캔더스타워`→Kendall's Tau, `mlist`→MNIST.
- 도구: `align.py`(정렬) · `retrieve.py`(검색) · `packets.py`(작업 패킷) · `insert.py`(마커 삽입) ·
  `apply_lecture.py`(판정 반영). 앞의 넷은 컨테이너 작업용, 마지막 하나만 저장소에 둔다.
